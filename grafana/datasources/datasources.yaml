apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: DS_TWEET_SENTIMENT SERVER METRICS
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: Loki
    type: loki
    uid: DS_LOKI
    access: proxy
    orgId: 1
    url: http://loki:3100
    editable: true